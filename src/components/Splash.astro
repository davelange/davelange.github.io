<script>
    import { animate } from "../lib/scenery";

    animate();
</script>

<canvas class="zdog-canvas" width="600" height="400"></canvas>

<style>
    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }
    .container {
        position: relative;
    }
    .zdog-canvas {
        margin: 2rem 0;
        opacity: 0;
        max-width: 90%;

        animation: fadeIn 1s 1s ease forwards;
        -webkit-mask-image: linear-gradient(
            to right,
            transparent,
            #000 20%,
            #000 80%,
            transparent
        );
    }
</style>
